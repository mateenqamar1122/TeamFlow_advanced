import React from 'react';
import { TeamReminderDashboard } from '@/components/TeamReminderDashboard';

const Reminders: React.FC = () => {
  return (
    <div className="container mx-auto px-4 py-8">
      <TeamReminderDashboard />
    </div>
  );
};

export default Reminders;
